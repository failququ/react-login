import React from 'react';
import { LoginHeader } from '../styles';

const Header = () => {
    return (
        <div>
            <LoginHeader>ONLY.</LoginHeader>
        </div>
    );
};

export default Header;