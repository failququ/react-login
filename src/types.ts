
export type TLoginForm = {
    login: string,
    password: string
}

export type TServerResponse = {
    logon: Boolean,
    message: String
}

export type TErrorMessage = {
    errorMessage: String
}